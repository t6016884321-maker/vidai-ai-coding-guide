# 安全说明

## API Key

本仓库不会要求用户提交 API Key。所有示例只使用占位符。

请勿：

- 将真实 API Key 提交到 Git；
- 在 Issue、群聊或截图中公开密钥；
- 把密钥硬编码进前端 JavaScript；
- 与他人共用个人密钥。

如果密钥可能泄露：

1. 立即在胃袋AI控制台撤销旧密钥；
2. 创建新密钥；
3. 检查 Git 历史、CI 日志和终端历史；
4. 更新本机环境变量。

请通过胃袋AI官方渠道反馈服务安全问题，不要在公开 Issue 中披露可利用细节或凭据。
